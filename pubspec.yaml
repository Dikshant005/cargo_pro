name: cargo_pro_assignments
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  get: ^4.7.2
  firebase_core: ^4.0.0
  firebase_auth: ^6.0.1
  http: ^1.5.0
  google_fonts: ^6.3.0
  responsive_builder: ^0.7.1
  json_annotation: ^4.9.0
  build_runner: ^2.7.0
  validators: ^3.0.0

flutter:
  uses-material-design: true
